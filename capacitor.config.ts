import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vtempest.automata',
  appName: 'Automated Automata',
  webDir: 'build'
};

export default config;
